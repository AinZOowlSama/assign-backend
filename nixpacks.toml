# nixpacks.toml
[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm db:generate", "pnpm --filter @repo/api build"]

[start]
cmd = "node apps/api/dist/index.js"